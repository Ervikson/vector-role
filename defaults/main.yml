---
# Vector version
vector_version: "0.51.1"

# Vector download URL
vector_download_url: "https://packages.timber.io/vector/{{ vector_version }}/vector_{{ vector_version }}-1_amd64.deb"

# Path to downloaded deb package
vector_deb_path: /tmp/vector.deb

# Vector service name
vector_service_name: vector
